import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pf2reference',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
